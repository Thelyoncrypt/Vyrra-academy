---
code: 6.2.2
title: 'Goal Decomposition: Breaking Complex Objectives into Agent Tasks'
summary: >-
  Effective goal decomposition requires understanding the task structure: is it
  sequential (each step depends on the previous), parallel (independent
  subtasks), conditional (branching based on intermediate results), or ite
outcomes: []
keyConcepts: []
estMinutes: 28
---
# Goal Decomposition: Breaking Complex Objectives into Agent Tasks

> Effective goal decomposition requires understanding the task structure: is it sequential (each step depends on the previous), parallel (independent subtasks), conditional (branching based on intermediate results), or ite

## Lesson

Effective goal decomposition requires understanding the task structure: is it sequential (each step depends on the previous), parallel (independent subtasks), conditional (branching based on intermediate results), or iterative (requiring refinement loops)?

The Decomposition Algorithm follows these steps: (1) Analyze the top-level goal for measurability - can success be defined as a binary or scalar outcome? (2) Identify independent sub-goals that can execute in parallel. (3) Identify dependencies - which sub-goals require outputs from others? (4) Define quality gates at each decomposition level. (5) Specify rollback procedures for each branch.

## Practice

### Multi-Agent Goal Decomposition. Take the objective "launch a new product line in Q3" and decompose it into agent-assigne

Multi-Agent Goal Decomposition. Take the objective "launch a new product line in Q3" and decompose it into agent-assigned subtasks. Define at least 20 subtasks across research, development, marketing, legal, and finance domains. Specify dependencies, parallelization opportunities, quality gates, and success metrics for each subtask.
