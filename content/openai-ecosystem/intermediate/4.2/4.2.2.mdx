---
code: 4.2.2
title: 'Anthropic API: Messages, Tool Use, and System Prompts'
summary: >-
  The Anthropic API uses a messages.create() endpoint with a separate system
  parameter (unlike OpenAI's inline approach): import anthropicclient =
  anthropic.Anthropic()message = client.messages.create( model="claude-sonnet
outcomes: []
keyConcepts: []
estMinutes: 8
---
# Anthropic API: Messages, Tool Use, and System Prompts

> The Anthropic API uses a messages.create() endpoint with a separate system parameter (unlike OpenAI's inline approach): import anthropicclient = anthropic.Anthropic()message = client.messages.create( model="claude-sonnet

## Lesson

The Anthropic API uses a messages.create() endpoint with a separate system parameter (unlike OpenAI's inline approach):

import anthropicclient = anthropic.Anthropic()message = client.messages.create( model="claude-sonnet-4-20250514", max_tokens=1024, system="You are a professional technical writer. Respond concisely.", messages=[{"role": "user", "content": "Explain RESTful APIs."}])

Cost optimization features: Prompt caching (writes at 1.25x base, reads at 0.1x - up to 90% savings); Batch API at 50% discount; and long-context surcharge eliminated March 2026. Claude Sonnet 4.6 at $3/$15 per 1M tokens delivers 99% of Opus's coding performance at 40% lower cost.
