---
code: 4.5.4
title: Mobile-First Responsive Patterns
summary: >-
  The modern responsive methodology follows an escalation workflow: intrinsic
  first (Flexbox, Grid, clamp()), container next (container queries measure
  parent, not viewport), media last (global viewport-wide shifts only).
outcomes: []
keyConcepts: []
estMinutes: 28
---
# Mobile-First Responsive Patterns

> The modern responsive methodology follows an escalation workflow: intrinsic first (Flexbox, Grid, clamp()), container next (container queries measure parent, not viewport), media last (global viewport-wide shifts only).

## Lesson

The modern responsive methodology follows an escalation workflow: intrinsic first (Flexbox, Grid, clamp()), container next (container queries measure parent, not viewport), media last (global viewport-wide shifts only).

/* Container query example */.card-container { container-type: inline-size; container-name: card;}@container card (min-width: 400px) {.card-header { display: flex; justify-content: space-between; }}

Container queries reached all browsers by 2024 and have transformed responsive design. The key principle: "Style for placement, not for platform".

## Practice

### Build a responsive dashboard with a design token system, 5 reusable components, and container queries. Use AI to generat

Build a responsive dashboard with a design token system, 5 reusable components, and container queries. Use AI to generate initial component code from Figma or description.
